# E5 S1 Native Replay V1 — independent simulator X-RED closure

Pre-result protocol: `945c4a6b878b27e385bfd50d1dafe428c262991d`. Implementation freeze: `fb3427fc90817769104101500b4a7bc739456fa8`, blob `36415bc95dd8d6dbd122022322906f93a0ff7d0a`. Exact run `35128262134`, job `104902586076`, artifact `10459987174`, original ZIP SHA256 `f7826fb81a8128298b7f3a0c519fa48928e046a9f8d6114372c503269a2db2aa`. Artifact ZIP independently downloaded and SHA matched GitHub digest. All 24 original carrier SHA256SUMS passed, re-generated query fingerprint matched v3. SMT witness projection returned SAT, extracted all requested DUT input and output values for q0..q5; no alternative model was selected. Icarus Verilog 12 compiled unchanged original RTL without FORMAL, and produced native output q0=q1 all zero, q2..q4 partially X, q5 all zero, so native agreement is UNRESOLVED, not scientifically SAT/UNSAT and not an RTL design defect.

Exact iverilog compile warning `pipemem.v:97: warning: @* found no sensitivities so it will never trigger.` Source line 97 is `always @(*)` with default parameter `OPT_ALIGNMENT_ERR=0`: its else branch assigns `misaligned=1'b0` without referencing any dynamic signals. Because Icarus never triggers this always block, native `misaligned` remains X, propagating through local-bus and `cyc` logic. This is a simulator scheduling/elaboration compatibility issue distinct from the historical `i_lock` assumption; never hide or edit it in the original V1 evidence.

The extracted historical trace q0..q5 uses q0 reset=1, q1 local pipemem request, q2 outstanding local cycle, q3 i_lock 0→1, q4 reset=1 while output local Wishbone cycle remains asserted, q5 cycle drops. Those are SMT model assertions only until native replay independently succeeds. Crucially the restored original assumption excludes the q2→q3 lock transition if its formal guard holds; no original property violation has been shown.

Allowed next prospective route: SAME frozen model-derived input vector, SAME unmodified original RTL, SAME default parameters, SAME sampling convention, but independently use Verilator simulation, which elaborates constant branches and will not depend on the Icarus empty sensitivity-list scheduling. Preserve exact simulator version/source and full logs, and compare every original DUT output bit q0..q5; any mismatch is RED, not permission to tune an alternative witness. Do NOT `force dut.misaligned`, edit RTL or regenerate a different favorable witness. Native 2-state Verilator initial-value semantics must be disclosed; this replays after q0 reset and checks all subsequent states. `PROPERTY_FAILURE_UNPROVEN` persists.
