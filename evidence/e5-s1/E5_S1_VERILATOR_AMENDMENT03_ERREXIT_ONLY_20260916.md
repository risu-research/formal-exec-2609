# E5 S1 Verilator Amendment03 — execution-only shell error-handling (pre-result)

Parent first attempt diagnostic RED `3602f70c9292614568e9f35823a0022edfa1c872`. Original Verilator protocol `d1273d1af9b5ee02628db1cbec1ba9b59989039a`, implementation script commit `fb3427fc90817769104101500b4a7bc739456fa8`, auditor commit `4c638511df7991beb189c3e81c90ecb853a7f139`, first workflow `7bc82e1df6f73c6bbeac15b54168bb4a9bc8cd81`.

Permit ONLY inserting `set +e` before the first original Icarus witness script expected to fail with preserved X output, then record rc, then restore `set -e` and require rc=1 and exact same frozen original witness/TB/query hashes. Shell GitHub uses bash `-e` by default; `set -uo pipefail` alone cannot turn it off. All other workflow lines, source/model/RTL/testbench/parameters/stimulus/sampling/Verilator compile invocation/targets remain unchanged. A failed Icarus diagnostic that is not the original exact X result must still fail the job. The original RED artifact remains untouched. Run Verilator only after passing all original evidence and witness identity gates. Zero scientific results from first attempt. Freeze this permission BEFORE editing the workflow.
