# Native Replay Amendment02 — independently elaborated constant branch with Verilator (pre-result)

First native Icarus diagnostic RED is sealed in commit `15c82331a2e2ace5766bc77047230f42a6c7647a`; original run `35128262134`, artifact `10459987174`, ZIP SHA256 `f7826fb81a8128298b7f3a0c519fa48928e046a9f8d6114372c503269a2db2aa`. Original protocol `945c4a6b878b27e385bfd50d1dafe428c262991d`, implementation `fb3427fc90817769104101500b4a7bc739456fa8`. Freeze BEFORE Verilator code or result. The first run's Icarus warning was empty `always @(*)` sensitivity of the default-parameter constant else assignment of `misaligned=0`; original simulator returned X at q2..q4. Not property failure or SMT counterexample.

Permit ONLY alternate independent simulator backend `verilator --binary --timing --top-module tb -Wno-fatal` on the exact `E_removed/pipemem.v` and exact existing `native_replay_tb.sv` produced from the FIRST Z3 SAT model of the ORIGINAL fixed `E_removed_observed_cycle.smt2` query. Do not modify original RTL, testbench, input/state vectors, clock sampling convention, defaults, formal assumption, query, or try a second model. Run frozen V1 witness extraction as diagnostic, verify it still exits on its expected X-RED, preserve stderr, and reuse its exact testbench and `witness_vectors.json` byte for byte; then run Verilator on those two unchanged files. Record Verilator and gcc versions, original hashes, compile output, simulation output, exact per-bit q0..q5 comparison, first formally forbidden assumption guard cycle, and a self-contained output SHA256SUMS. An X, unknown bit, incomplete trace, mismatch, compile error, or corrupted artifact is RED. Do not alter the testbench or `force dut.misaligned`; no changed simulator stimulus is allowed. Verilator's 2-state initialization and post-reset check limitations must be disclosed. No historical safety property or shipped hardware defect claim follows automatically from any positive output trace.

A successful Verilator replay would verify one post-hoc short input/output history from the original RTL independently of Yosys's RTL translation but NOT prove the complete set of admissible histories, 40-state nonreachability, original formal property failure, or hardware silicon behavior. The decisive distinction remains: the formal assumption changes which physical-input histories are admissible for a proof, not the design outputs under a fixed physical input.
