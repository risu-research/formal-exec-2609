# E5 S1 v3 independent replay protocol (pre-result)

Parent public commit: `0f0fd55e83b438f1d6d0e1f1c39a07971d176855`. Original ZipCPU child: `d511239e19be8fcc7f340a64554ea93699637e62`. Evidence carrier: Actions run `35122112448`, artifact `10457767655`, GitHub ZIP SHA-256 `9662c2a052d58312cca8d6906a36ca5aa9b2554b6e345344da6349ae60e0674f`. Source and SMT hashes are frozen in `smoke/e5_s1_incremental_monitor_free_v3.py`. The original V2 ZIP was independently checked against the GitHub ZIP digest, with SHA256SUMS verified before this protocol.

## Question, targets, and horizons

Use the two authenticated, monitor-free Yosys SMT2 models without modifying their bytes. Only the historical `i_lock` assumption is different between source arms. Separate (a) direct DUT-output target: `!o_busy && !o_wb_stb_gbl && !o_wb_stb_lcl && (o_wb_cyc_gbl || o_wb_cyc_lcl)`; (b) same endpoint plus formal helper `f_outstanding == 0 && !f_cyc`; (c) post-hoc six-state trace replay `observed_cycle`; (d) contradictory negative control. Do not equate any of these with a historical safety-property violation.

Test the original six-state witness prefix `q0..q5` FIRST for both arms, followed by the *separately retained original* 40-state bound `q0..q39`, for both solvers. The six-state check is only an early positive/prefix audit; it cannot replace the 40-state restored-arm nonreachability claim. A 90-second per-solver/per-mode limit applies to the full 40-state incremental replay; timeout, unknown, parse error, or missing result means UNRESOLVED, never UNSAT. No inference from a missing later result. Both solvers must agree on the appropriate endpoints, with normal exits and passing negative controls, for promotion. A SAT must have a reconstructed model/trace checked against original transition, init and assumption constraints before a certificate is issued. Reuse of the shared Yosys translation is a residual common-mode risk even when the solvers agree.

## Integrity and no outcome shopping

Use `actions/download-artifact` to fetch the specified artifact from the specified prior run, verify all internal `SHA256SUMS`, verify frozen source and model bytes using the existing v3 program, and preserve logs, exit codes, solver versions, hash manifest and query bytes. Do not modify historical RTL, helper modules, formal assumptions, original monitors, pinned model files, or endpoint definitions. The prospective early prefix is exactly six states because the previously observed witness uses `q0..q5`, not because an outcome has been observed in this replay. Run both arms and both solvers regardless of positive or negative results. A failed run must preserve evidence; CI green does not equal a scientific verdict.

If a v3 SAT exists but the same timed input sequence has not been replayed independently in the original RTL, label it only a monitor-free formal-model result. If direct-output reachability is SAT under both arms, do not claim that this broad output-only predicate distinguishes them; narrow to the pre-existing six-state trace replay and disclose post-hoc target selection. For the restored-arm 40-state bound, all incremental checks must be UNSAT (not merely the last check), and no state outside this bound is claimed. A further independent RTL simulation and ideally an independently produced translator/netlist remain future evidence gates, not presumed successes.
