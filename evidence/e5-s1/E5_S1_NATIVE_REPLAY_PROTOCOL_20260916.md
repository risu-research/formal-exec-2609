# E5 S1 next gate: native RTL input-vector replay (prospective, no outcome yet)

Parent qualified v3 closure: `fb57007db6714d5a85bf35e95421a97d97284a4f`; no changes to any v3 evidence. Objective: challenge the residual common-mode Yosys interpretation of the exact, already-observed six-state trace, not search for a prettier output difference. The two source arms' synthesizable RTL are byte-identical once FORMAL is disabled; the formal assumption changes *admissible histories*, not the physical transition function.

Input: immutable v2 artifact ID `10457767655` on run `35122112448`, outer ZIP digest `9662c2a052d58312cca8d6906a36ca5aa9b2554b6e345344da6349ae60e0674f`; model and source byte identities verified by frozen v3 generator. Do NOT modify RTL, hierarchy, model, target, trace order, input values or formal assumption. Take ONE Z3 SAT model for the previously fixed E_removed `observed_cycle` q0..q5 query; enable model production only in the *separate witness extraction copy* of this same query, and ask for all DUT input and output pins at q0..q5. Preserve the exact new SMT query and solver stdout bytes and versions.

Generate a plain SystemVerilog testbench (no `FORMAL`/`PIPEMEM` compile defines and no `fwb_master` helper) for unchanged `E_removed/pipemem.v`, default parameterization. Reapply the exact extracted clock-step input assignments at each q0..q5; explicitly treat Yosys's q_i SMT state as pre-edge sample i and transition q_i→q_(i+1) as one positive edge. Run the source through independent iverilog/vvp and compare, cycle for cycle, the original DUT `o_busy`, `o_wb_stb_gbl/lcl`, `o_wb_cyc_gbl/lcl`, and internal `cyc` against independently retrieved SMT model values. Preserve full vectors, native output, exact input stimulus and simulator/tool hashes. Verify the observed-cycle predicate directly from native outputs at q2..q5 and the original historic assumption trigger from the *input plus native DUT state*, including the FIRST disallowed cycle in the restored profile. If native mismatch or unknown output occurs, record RED rather than altering vectors or setup. No arbitrary clock alignment or rerolling new models until an existing model happens to pass.

Native RTL simulation does NOT independently certify the SMT model's formal helper `f_outstanding` or original proof property. For a positive native run, independently check the helper only by explicitly reconstructing transactions or a separately checked original helper. Then determine whether the originally checked property is violated on the same trace; otherwise keep `PROPERTY_FAILURE_UNPROVEN`. A native mismatch is not evidence of a bug in historic hardware without ruling out formal/simulation initial-state and scheduling differences. The already-observed target is post-hoc; do not call this a new blind discovery.
