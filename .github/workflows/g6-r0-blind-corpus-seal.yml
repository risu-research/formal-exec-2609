name: g6-r0-blind-corpus-seal

on:
  workflow_dispatch:
  push:
    paths:
      - 'smoke/g6_r0_blind_corpus_seal.py'
      - '.github/workflows/g6-r0-blind-corpus-seal.yml'

permissions:
  contents: read

jobs:
  corpus_seal:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - name: Checkout public runner
        uses: actions/checkout@v4

      - name: Preflight R0 information firewall
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          s=Path('smoke/g6_r0_blind_corpus_seal.py').read_text()
          forbidden=['--format=%s','--format=%B','--format=%b','git log --oneline']
          bad=[x for x in forbidden if x in s]
          assert not bad, bad
          assert '4c8218fe664816f01653e483a6f205428eac33c3' in s
          assert '7ec2f62f2c68ac54879f1e25ec014e89a95b6ba8' in s
          print('R0 firewall preflight PASS')
          PY

      - name: Clone ZipCPU history blobless
        run: |
          git -c protocol.version=2 clone --filter=blob:none --no-checkout --single-branch https://github.com/ZipCPU/zipcpu.git zipcpu
          git -C zipcpu rev-parse HEAD

      - name: Clone frozen riscv-formal identity history blobless
        run: |
          git -c protocol.version=2 clone --filter=blob:none --no-checkout --single-branch https://github.com/SymbioticEDA/riscv-formal.git riscv-formal
          git -C riscv-formal rev-parse HEAD
          git -C riscv-formal remote -v

      - name: Clone OpenTitan history blobless
        run: |
          git -c protocol.version=2 clone --filter=blob:none --no-checkout --single-branch https://github.com/lowRISC/opentitan.git opentitan
          git -C opentitan rev-parse HEAD

      - name: Execute selection-only R0
        run: |
          rm -rf g6-r0-evidence
          mkdir -p g6-r0-evidence
          python3 smoke/g6_r0_blind_corpus_seal.py \
            --zipcpu zipcpu \
            --riscv-formal riscv-formal \
            --opentitan opentitan \
            --out g6-r0-evidence | tee g6-r0-evidence/runner.log

      - name: Seal provenance
        if: always()
        run: |
          mkdir -p g6-r0-evidence
          printf '%s\n' "$GITHUB_SHA" > g6-r0-evidence/public-runner-head.txt
          printf '%s\n' '4c8218fe664816f01653e483a6f205428eac33c3' > g6-r0-evidence/g6-preselection-authority.txt
          printf '%s\n' '7ec2f62f2c68ac54879f1e25ec014e89a95b6ba8' > g6-r0-evidence/evaluation-freeze-authority.txt
          git -C zipcpu rev-parse HEAD > g6-r0-evidence/zipcpu-fetch-head.txt 2>/dev/null || true
          git -C riscv-formal rev-parse HEAD > g6-r0-evidence/riscv-formal-fetch-head.txt 2>/dev/null || true
          git -C opentitan rev-parse HEAD > g6-r0-evidence/opentitan-fetch-head.txt 2>/dev/null || true
          git -C riscv-formal remote -v > g6-r0-evidence/riscv-formal-remotes.txt 2>/dev/null || true
          sha256sum smoke/g6_r0_blind_corpus_seal.py > g6-r0-evidence/runner-source.sha256
          find g6-r0-evidence -type f ! -name SHA256SUMS.txt -print0 | sort -z | xargs -0 sha256sum > g6-r0-evidence/SHA256SUMS.txt || true

      - name: Upload G6 R0 evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: g6-r0-blind-corpus-seal-evidence
          path: g6-r0-evidence/
          if-no-files-found: error
          retention-days: 90
